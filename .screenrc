escape ^q^q
caption always "%?%F%{= wk}%:%{= ww}%?%-w%{=u Wk}%n %t%{-}%+w"
defscrollback 10000
bind w windowlist -b
zombie "q^M"

# ハングアップしたときにscreenは自動的にデタッチ
autodetach on
# 起動時に著作権表示
startup_message off